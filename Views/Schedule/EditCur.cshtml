@using Microsoft.AspNetCore.Identity;
@using HSEPractice2.Areas.Identity.Data
@inject UserManager<ApplicationUser> UserManager
@inject SignInManager<ApplicationUser> SignInManager
@model CurrentShift

<form asp-action="EditCur">
	<div class="border p-3 mt-4">
		<div class="row pb-2">
			<h2 class="text-primary">
				Желаете изменить текущую смену?
			</h2>
			<hr />
		</div>

		<div asp-validation-summary="ModelOnly" class="text-danger"></div>
		<input type="hidden" asp-for="CurrentShiftId" />


		<div class="mb-3">
			<label asp-for="Shift">Смена</label>
			<select asp-for="Shift" asp-items="ViewBag.Shi" class="form-control"></select>
			<span asp-validation-for="Shift" class="text-danger"></span>
		</div>
		<div class="mb-3">
			<label asp-for="Employee">Идентификационный номер сотрудника</label>
			<input asp-for="Employee" readonly class="form-control"/>
			<span asp-validation-for="Employee" class="text-danger"></span>
		</div>
			@*		<div class="mb-3">
			<label asp-for="EmployeeNavigation.EmployeeSurname">Сотрудник</label>
			@if (Model.EmployeeNavigation != null)
			{
				var employeeName = Model.EmployeeNavigation.EmployeeName;
				var employeePatronymic = Model.EmployeeNavigation.EmployeePatronymic;

				<input asp-for="EmployeeNavigation.EmployeeSurname" readonly class="form-control" value="@Model.EmployeeNavigation.EmployeeSurname @(employeeName != null && employeeName.Length > 0 ? employeeName.Substring(0, 1) : string.Empty) @(employeePatronymic != null && employeePatronymic.Length > 0 ? employeePatronymic.Substring(0, 1) : string.Empty)" />
			}
			else
			{
				<input readonly class="form-control" value="Нет информации о сотруднике" />
			}*@
		@*</div>*@


		<div class="form-group">
			<input type="submit" value="Изменить" class="btn btn-primary" style="width:150px" />
			<a asp-controller="Schedule" asp-action="Index" class="btn btn-secondary" style="width:200px">
				Вернуться к списку
			</a>
		</div>
	</div>
</form>



@section Scripts{
	@{
		<partial name="_ValidationScriptsPartial.cshtml" />
	}
}


